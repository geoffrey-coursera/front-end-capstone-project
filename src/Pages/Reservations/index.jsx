import BookingForm from './BookingForm';

import './Reservations.scss';

export { Reservations as default };

const Reservations = () => (
    <main id="Reservation">
        <BookingForm />
    </main>
)